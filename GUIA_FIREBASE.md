# 🔥 Guía de Conexión: Google Firebase (Método Infalible)

Si no encuentras los botones en la pantalla de inicio, usa este camino que nunca falla:

## PASO 1: Ir a la Configuración

1. Mira la **esquina superior izquierda** de la pantalla de Firebase.
2. Haz clic en el **Icono de Engranaje (⚙️)** que está junto a "Descripción general".
3. En el menú que se abre, selecciona la primera opción: **"General"**.

## PASO 2: Buscar tus Apps

1. Baja (haz scroll) hasta el final de esa página.
2. Verás una sección llamada **"Tus apps"**.
3. Si no hay ninguna, verás el icono circular **Web `</>`**. Haz clic ahí.
    * Registra la app con el nombre `VecyWeb`.
4. Si ya existe, verás la app listada ahí.

## PASO 3: Copiar el SDK (Las Credenciales)

En esa misma sección de "Tus apps", selecciona la opción **"Configuración"** o **"SDK setup and configuration"**.
Verás un cuadro de código que dice `const firebaseConfig = { ... }`.

Copia **solo lo que está dentro de las llaves** y pégalo en el chat. Ejemplo:

```javascript
apiKey: "AIzaSyD...",
authDomain: "vecy-plataforma.firebaseapp.com",
projectId: "vecy-plataforma",
storageBucket: "...",
messagingSenderId: "...",
appId: "..."
```
